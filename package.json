{
  "name": "iptv",
  "private": true,
  "packageManager": "pnpm@9.7.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "format": "biome format --write .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "webos:package": "pnpm --filter @iptv/web build && ares-package apps/web/dist -o apps/web/release"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "turbo": "^2.5.0"
  }
}
