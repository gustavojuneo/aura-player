# Landing Components Guide

Components in this directory are private to the landing page at `/`.

If a landing component becomes useful on another page, promote it to
`src/components/` (or `src/components/ui/` if it is a generic UI element) and
remove the page-only assumption from its API.
