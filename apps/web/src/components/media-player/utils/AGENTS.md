# Media Player Utilities Guide

This directory contains pure helpers private to the Media Player family. Keep
these modules free of React state, effects, browser subscriptions, and JSX.
Fixed application-wide options still belong in `src/utils/constants.ts`.
