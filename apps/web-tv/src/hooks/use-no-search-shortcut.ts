import type { RefObject } from "react";

export function useSearchShortcut(
  _searchInputRef: RefObject<HTMLInputElement | null>,
) {
  // Search is activated through the TV interface and remote navigation.
}
